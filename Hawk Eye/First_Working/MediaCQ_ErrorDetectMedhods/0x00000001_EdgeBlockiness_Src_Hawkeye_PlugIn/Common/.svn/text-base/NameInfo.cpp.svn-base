#include "StdAfx.h"
#include "NameInfo.h"

CNameInfo::CNameInfo(void)
{
	memset( this, 0, sizeof(NAMEINFO) );
}
CNameInfo::CNameInfo( const CNameInfo& info )
{
	memcpy( this, &info, sizeof(NAMEINFO) );
}

CNameInfo::CNameInfo( const NAMEINFO&  info )
{
	memcpy( this, &info, sizeof(NAMEINFO) );
}

void CNameInfo::operator=( const CNameInfo& info )
{
	memcpy( this, &info, sizeof(NAMEINFO) );
}

void CNameInfo::operator=( const NAMEINFO&  info )
{
	memcpy( this, &info, sizeof(NAMEINFO) );
}
